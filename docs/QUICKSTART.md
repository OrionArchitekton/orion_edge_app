# Quickstart
USB → Ubuntu Server → tailscale up --authkey=... --ssh
git clone or unpack scaffold into repo → fill env → docker compose up
Import n8n flows → creds → test booking/returns → confirm Langfuse traces

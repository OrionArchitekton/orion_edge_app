### MCP health (local)
GET http://mcp.localhost/healthz

### MCP health (external Cloudflare Worker)
GET https://mcp.orionbot.online/healthz

### Langfuse ping (UI path)
GET http://ops.localhost/langfuse

### n8n index
GET http://ops.localhost/n8n

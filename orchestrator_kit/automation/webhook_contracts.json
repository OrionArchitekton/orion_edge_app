
## webhook\_contracts.json

```json
{
  "unknown_event": {
    "type": "unknown_event",
    "client": "string",
    "channel": "web|messenger",
    "user_id": "string",
    "question": "string",
    "email_captured": "boolean",
    "link_transcript": "url",
    "created_at_utc": "iso8601"
  },
  "interaction": {
    "type": "interaction",
    "client": "string",
    "topic": "string",
    "matched_faq": "boolean",
    "memory": { "hit": "boolean" },
    "response_text": "string",
    "timestamp": "iso8601"
  },
  "deploy_event": {
    "type": "deploy_event",
    "client": "string",
    "version": "semver",
    "changes": ["string"],
    "rollback": "url or text",
    "actor": "string",
    "published_at_utc": "iso8601"
  },
  "kpi_summary": {
    "type": "kpi_summary",
    "client": "string",
    "period": "YYYY-WW",
    "interactions": "number",
    "resolution_rate": "number",
    "leads": "number",
    "handoffs": "number",
    "top_topics": ["string"],
    "wow_deltas": {"interactions":"%","resolution_rate":"%"}
  }
}
```

**Mapping notes:** `interaction` feeds Zap 1 (Sheets + memo recall), `unknown_event` drives Zap 2, `kpi_summary` powers Zap 3, and `deploy_event` comes from Zap 5. Post all to Slack webhooks per `automation_specs.md`.

##### PASS/FAIL — webhook\_contracts.json

- PASS if: JSON validates, fields populated by Zaps, Slack receives correctly‑typed payloads, links resolvable. Else FAIL.
